'use client';
import { useEffect, useState } from 'react'; import { supabase } from '@/lib/supabaseClient';
export default function RankingPage(){ const [rows,setRows]=useState<any[]>([]); useEffect(()=>{load();},[]); async function load(){ const {data:userData}=await supabase.auth.getUser(); if(!userData.user){window.location.href='/login';return;} const {data,error}=await supabase.from('leaderboard').select('*'); if(error)alert(error.message); setRows(data||[]);} return <div><h1>Rangliste</h1><p className='muted'>5 Punkte exaktes Ergebnis · 3 Punkte Tordifferenz · 2 Punkte Tendenz</p><table><thead><tr><th>Platz</th><th>Spieler</th><th>Punkte</th><th>gewertete Tipps</th></tr></thead><tbody>{rows.map((r,i)=><tr key={r.player_id}><td>{i+1}</td><td>{r.name}</td><td>{r.points}</td><td>{r.counted_tips}</td></tr>)}</tbody></table></div>; }
